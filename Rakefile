begin
  require 'jeweler'
  Jeweler::Tasks.new do |gem|
    gem.name = "partial_dependencies"
    gem.summary = "Rails: Dependency graph of Rails views and partials"
    gem.email = "paul.t.hinze@gmail.com"
    gem.homepage = "http://github.com/msales/partial_dependencies"
    gem.authors = ["Paul Hinze"]
  end
rescue LoadError
  puts "Jeweler, or one of its dependencies, is not available. Install it with: sudo gem install technicalpickles-jeweler -s http://gems.github.com"
end